<form class="{{cssClass}} race os-item-form" autocomplete="off">
  <header class="sheet-header os-item-header">
    <img class="os-item-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />

    <div class="os-item-headmeta">
      <h1 class="os-item-title">
        <input name="name" type="text" value="{{item.name}}" placeholder="{{ localize 'Name' }}" />
      </h1>
    </div>
  </header>

  <div class="os-item-body os-item-grid">
    <div class="os-col">
      <section class="os-panel os-panel--pad">
        <h3 class="os-panel-title">Описание</h3>

        <div class="os-form-grid">
          <div class="os-field">
            <label>Средняя продолжительность жизни:</label>
            <input name="data.lifeExpectancy" type="text" value="{{data.lifeExpectancy}}" />
          </div>

          <div class="os-field">
            <label>Размер:</label>
            <input name="data.size" type="text" value="{{data.size}}" />
          </div>

          <div class="os-field">
            <label>Внешний вид:</label>
            <textarea name="data.distinctiveTraits" class="os-textarea">{{data.distinctiveTraits}}</textarea>
          </div>

          <div class="os-field">
            <label>Отличительные черты:</label>
            <textarea name="data.distinctiveTraits" class="os-textarea">{{data.distinctiveTraits}}</textarea>
          </div>

          <div class="os-field">
            <label>Предрасположенности:</label>
            <textarea name="data.predispositions" class="os-textarea">{{data.predispositions}}</textarea>
          </div>

          <div class="os-field">
            <label>Культура:</label>
            <textarea name="data.culture" class="os-textarea">{{data.culture}}</textarea>
          </div>

          <div class="os-field">
            <label>Стереотипы:</label>
            <textarea name="data.stereotypes" class="os-textarea">{{data.stereotypes}}</textarea>
          </div>

          <div class="os-field">
            <label>Место в современном обществе:</label>
            <textarea name="data.placeInModernSociety" class="os-textarea">{{data.placeInModernSociety}}</textarea>
          </div>
        </div>
      </section>
    </div>

    <div class="os-col">
      <section class="os-panel os-panel--pad">
        <div class="os-panel-title-row advantage-field">
          <h3 class="os-panel-title">Параметры</h3>
          <button type="button" class="modify-advantage-button os-small-btn">+ Параметр</button>
        </div>

        <div class="additional-advantages os-pill-list">
          {{#each data.additionalAdvantages as |curAd index| }}
            <div class="advantage-char os-pill" data-index="{{index}}">
              <span class="os-pill-text">{{formatAdditionalAdvantage curAd}}</span>
              <a type="button" class="advantage-remove-characteristic os-pill-action">❌</a>
            </div>
          {{/each}}
          {{#unless data.additionalAdvantages.length}}
            <div class="os-empty">Нет параметров</div>
          {{/unless}}
        </div>
      </section>

      <section class="os-panel os-panel--pad">
        <h3 class="os-panel-title">Навыки расы</h3>

        <div class="os-form-row" style="margin-bottom: 8px;">
          <label class="os-checkbox" style="display:flex; gap:8px; align-items:center;">
            <input type="checkbox" name="data.selectOneSkill" {{#if data.selectOneSkill}}checked{{/if}} />
            <span>Выбирать один навык при переносе</span>
          </label>
        </div>

        <div class="drop-area skills-drop">
          <p class="os-help">Перетащи навык сюда</p>
        </div>

        <div class="skills-list os-list">
          {{#each data.Skills as |skill|}}
            <div class="skill-row os-row" data-skill-id="{{skill._id}}">
              <span class="skill-link" data-skill-id="{{skill._id}}">{{skill.name}}</span>
              <button type="button" class="delete-skill-button os-small-btn" data-array="Skills" data-id="{{skill._id}}">Удалить</button>
            </div>
          {{/each}}
          {{#unless data.Skills.length}}
            <div class="os-empty">Нет навыков</div>
          {{/unless}}
        </div>
      </section>
    </div>
  </div>
</form>
